/**
 * Clase que representa a un usuario o (actor del sistema)
 *
 * @author Jose Luis Obiang Ela Nanguan
 * @version 1.0
 */


/**
 * constructor parametrizado
 */
class Usuario {
    /**
     * Constructor parametrizado
     *
     * @param {string} username               Variable de tipo cadena. Recibe el nombre de un usuario
     * @param {string} password Variable de tipo cadena.        Recibe la contraseña de un usuario
     */
    constructor(username, password) {
        this.username = username;
        this.password = password;
    }
}

